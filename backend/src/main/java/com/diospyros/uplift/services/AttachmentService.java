/*
 * Created on 2023-10-21 ( 17:17:38 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 4.1.0
 */
package com.diospyros.uplift.services;

import java.util.List;
import java.util.Optional;


import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;

import com.diospyros.uplift.persistence.entities.Attachment;
import com.diospyros.uplift.persistence.repositories.AttachmentRepository;
import com.diospyros.uplift.dto.AttachmentDTO;
import com.diospyros.uplift.services.commons.GenericService;

/**
 * REST service for entity "Attachment" <br>
 * 
 * This service provides all the necessary operations required by the REST controller <br>
 * 
 * @author Telosys
 *
 */
@Service
public class AttachmentService extends GenericService<Attachment, AttachmentDTO> {

	private static final Logger logger = LoggerFactory.getLogger(AttachmentService.class);

	private final AttachmentRepository repository; // injected by constructor

	/**
	 * Constructor (usable for Dependency Injection)
	 * 
	 * @param repository the repository to be injected
	 */
	public AttachmentService(AttachmentRepository repository) {
		super(Attachment.class, AttachmentDTO.class);
		this.repository = repository;
	}
	
	/**
	 * Returns the entity ID object from the given DTO
	 *
	 * @param dto
	 * @return
	 */
	private Integer getEntityId(AttachmentDTO dto) {
		return dto.getId();
	}

	/**
	 * Finds all occurrences of the entity
	 *
	 * @return
	 */
	public List<AttachmentDTO> findAll() {
		logger.debug("findAll()");
		Iterable<Attachment> all = repository.findAll();
		return entityListToDtoList(all);
	}

	/**
	 * Finds the entity identified by the given PK
	 *
	 * @param id 
	 * @return the entity or null if not found
	 */
	public AttachmentDTO findById(int id) {
		Integer entityId = id;
		logger.debug("findById({})", entityId);
		Optional<Attachment> optionalEntity = repository.findById(entityId);
		return entityToDto(optionalEntity);
	}

	/**
	 * Saves the given entity with the given PK <br>
	 * "UPSERT" operation (updated if it exists or created if it does not exist)
	 *
	 * @param id 
	 * @param dto 
	 */
	public void save(int id, AttachmentDTO dto) {
		Integer entityId = id;
		logger.debug("save({},{})", entityId, dto);
		// force PK in DTO (just to be sure to conform with the given PK) 
		dto.setId(id);
		repository.save(dtoToEntity(dto));
	}

	/**
	 * Updates the given entity if it exists
	 *
	 * @param dto
	 * @return true if updated, false if not found
	 */
	public boolean update(AttachmentDTO dto) {
		logger.debug("update({})", dto);
		if (repository.existsById(getEntityId(dto))) {
			repository.save(dtoToEntity(dto));
			return true; // find and updated
		} else {
			return false; // not found (not updated)
		}
	}

	/**
	 * Updates partially the given entity if it exists
	 *
	 * @param id 
	 * @param dto
	 * @return true if updated, false if not found
	 */
	public boolean partialUpdate(int id, AttachmentDTO dto) {
		Integer entityId = id;
		logger.debug("partialUpdate({}, {})", entityId, dto);
		Optional<Attachment> optionalEntity = repository.findById(entityId);
		if (optionalEntity.isPresent()) {
			Attachment entity = optionalEntity.get();
			// implement here the partial update
			// entity.setXxx(dto.getXxx());
			// etc ...
			repository.save(entity);
			return true; // find and updated
		} else {
			return false; // not found (not updated)
		}
	}

	public Attachment create(AttachmentDTO dto) {
		logger.debug("create({})", dto);
		// auto-generated Primary Key
		return repository.save(dtoToEntity(dto));
	}

	/**
	 * Deletes an entity by its PK
	 *
	 * @param id 
	 * @return true if deleted, false if not found
	 */
	public boolean deleteById(int id) {
		Integer entityId = id;
		logger.debug("deleteById({})", entityId);
		if (repository.existsById(entityId)) {
			repository.deleteById(entityId);
			return true; // find and deleted
		} else {
			return false; // not found (not deleted)
		}
	}

	// -----------------------------------------------------------------------------------------
	// Specific "finders"
	// -----------------------------------------------------------------------------------------
/***
	public List<AttachmentDTO> findByTitle(String title) {
		logger.debug("findByTitle({})", title);
		// List<Attachment> list = repository.findByTitle(title);
		List<Attachment> list = repository.findByTitleContaining(title);
		return entityListToDtoList(list);
	}

	public List<AttachmentDTO> findByPrice(BigDecimal price) {
		logger.debug("findByPrice({})", price);
		// List<Attachment> list = repository.findByTitle(title);
		List<Attachment> list = repository.findByPrice(price);
		return entityListToDtoList(list);
	}

	public List<AttachmentDTO> findByTitleAndPrice(String title, BigDecimal price) {
		logger.debug("findByTitleAndPrice({}, {})", title, price);
		List<Attachment> list = repository.findByTitleContainingAndPrice(title, price);
		return entityListToDtoList(list);
	}
***/
}
